import React from 'react';

const Login = () => {
    return (
        <div>
            <h2>Login</h2>
            <p>Login form for existing users.</p>
        </div>
    );
};

export default Login;
